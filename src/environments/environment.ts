// Development environment settings
export const environment = {
  production: false,
  // backend JSON Server base URL (includes /api because backend mounts router at /api)
  apiUrl: 'https://json-server-senteranga.onrender.com/api'
};
